import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.unil.sprintcalendar',
  appName: 'sprint-calendar-app',
  webDir: 'dist',
};

export default config;
